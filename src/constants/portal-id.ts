// Portal mapping id
// Located in "public/index.html"
export const portalId = {
  root: 'root',
  loader: 'loader-root',
  popper: 'popper-root',
  modal: 'modal-root',
  modalBackdrop: 'modal-backdrop-root',
  subModal: 'sub-modal-root',
  subModalBackdrop: 'sub-modal-backdrop-root',
  alert: 'alert-root',
  alertBackdrop: 'alert-backdrop-root',
};
